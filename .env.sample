# FreeSound Environment Configuration
# Copy this file to .env and update the values as needed

# Frontend Configuration
# The API URL that the frontend will use to connect to the backend
# For Docker: use the backend service name (http://backend:8000) or your server IP
# For local development: use http://localhost:8000
NEXT_PUBLIC_API_URL=http://backend:8000

# Backend Configuration
# Python unbuffered output (set to 1 for better logging in Docker)
PYTHONUNBUFFERED=1

# Logging path (empty for in the local dir)
LOG_PATH=